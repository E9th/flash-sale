# ⚡ Flash Sale System (High Concurrency Architecture)
ระบบจำลองการขายสินค้าแบบ **Flash Sale** ที่ออกแบบมาเพื่อรองรับการกดสั่งซื้อจำนวนมากพร้อมกัน (High Throughput) โดยเน้นความถูกต้องของสต็อก (Data Consistency) และการตอบสนองที่รวดเร็ว (Low Latency) ผ่านสถาปัตยกรรมแบบ **Asynchronous Event-Driven**

## 📖 ภาพรวมระบบ (System Overview)
โปรเจกต์นี้แก้ปัญหาคลาสสิกของระบบ E-commerce เวลาจัดโปรโมชั่นลดราคา ซึ่งมักเจอสภาวะ **"Race Condition"** (แย่งกันซื้อจนสต็อกติดลบ) และ **"Database Bottleneck"** (ฐานข้อมูลล่มเพราะรับ Transaction ไม่ไหว)

### หลักการทำงาน (Architecture Flow)
1. **Load Balancing:** `Nginx` ทำหน้าที่กระจาย Request ไปยัง `API Server` (ซึ่งรันไว้ 3 Replicas)
2. **Request Handling (API):**
* เมื่อมีคำสั่งซื้อเข้ามา (`POST /api/buy`) ระบบจะไม่บันทึกลง Database ทันที
* **Stock Check:** ตัดสต็อกผ่าน `Redis` (Atomic Operation) เพื่อความเร็วและป้องกัน Race Condition เบื้องต้น
* **Queueing:** หากยังมีของ ส่งข้อมูลคำสั่งซื้อไปพักไว้ที่ `RabbitMQ` แล้วตอบกลับ User ทันทีว่า "รับเรื่องแล้ว" (HTTP 202)
3. **Background Processing (Worker):**
* `Worker` จะทยอยดึงคำสั่งซื้อจาก Queue มาประมวลผล
* **Database Transaction:** เปิด Transaction ใน `PostgreSQL` เพื่อบันทึก Order และตัดสต็อกจริง
4. **Real-time Feedback:**
* เมื่อตัดของสำเร็จ Worker จะส่งข้อมูลสต็อกล่าสุดผ่าน `Redis Pub/Sub`
* API Server จะกระจายข้อมูลนี้ผ่าน **WebSocket** ไปยังหน้าเว็บ Frontend เพื่ออัปเดตตัวเลขสต็อกแบบ Real-time
---

## 🛠️ Tech Stack & Tools
* **Language:** Go (Golang) v1.24
* **Web Framework:** Fiber v2 (Fast HTTP & WebSocket)
* **Database:** PostgreSQL (GORM ORM)
* **Cache & Pub/Sub:** Redis
* **Message Queue:** RabbitMQ
* **Load Balancer:** Nginx
* **Infrastructure:** Docker & Docker Compose
* **Testing:** k6 (Load Testing)
---

## 🚀 วิธีการติดตั้งและรัน (Getting Started)
### Prerequisites

* [Docker](https://www.docker.com/) และ Docker Compose ติดตั้งอยู่ในเครื่อง

### ขั้นตอนการรัน (Installation)

1. **Clone หรือเตรียมไฟล์**
ตรวจสอบให้แน่ใจว่าไฟล์ทั้งหมดอยู่ในโครงสร้างโฟลเดอร์ที่ถูกต้อง
2. **Start System**
รันคำสั่ง Docker Compose เพื่อสร้างและเริ่ม Container ทั้งหมด (Database, Redis, RabbitMQ, API, Worker, Nginx)
```bash
docker-compose up --build

```
*รอสักครู่จนกว่าจะเห็น Log ว่า `✅ Connected to Database!` และ `✅ Connected to RabbitMQ!*`
3. **เข้าใช้งาน**
* เปิด Browser ไปที่: [http://localhost](https://www.google.com/search?q=http://localhost)
* คุณจะเห็นหน้า Dashboard แสดงสินค้า iPhone 15 Pro พร้อมกราฟแสดงสต็อกแบบ Real-time

---

## 🧪 การทดสอบระบบ (Load Testing)
โปรเจกต์นี้มาพร้อมกับสคริปต์ `k6` เพื่อจำลองผู้ใช้งานจำนวนมากถล่มกดซื้อพร้อมกัน
1. **ติดตั้ง k6** (ถ้ายังไม่มี)
* Mac: `brew install k6`
* Windows: `choco install k6` หรือดาวน์โหลด Binary

2. **รันการทดสอบ**
```bash
k6 run loadtest.js

```
*การตั้งค่าปัจจุบัน:* จำลองผู้ใช้ (VUs) 100 คน ยิงรัวๆ เป็นเวลา 10 วินาที
3. **ผลลัพธ์ที่คาดหวัง**
* หน้าเว็บ Frontend จะแสดงจำนวนสต็อกลดลงอย่างรวดเร็วแบบ Real-time
* ใน Terminal ของ k6 ควรเห็น Status `202` (รับเรื่อง) หรือ `400` (ของหมด)
* **ต้องไม่มี Status 500** (Internal Server Error) หรือสต็อกติดลบใน Database
---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
flash-sale-system/
├── cmd/
│   ├── api/
│   │   └── main.go       # Entry point ของ API Server (HTTP/WebSocket)
│   └── worker/
│       └── main.go       # Entry point ของ Background Worker
├── internal/
│   ├── cache.go          # การเชื่อมต่อ Redis
│   ├── database.go       # การเชื่อมต่อ PostgreSQL & GORM Models
│   ├── rabbitmq.go       # การเชื่อมต่อ RabbitMQ & Publish Function
│   └── ...
├── nginx/
│   └── nginx.conf        # Config ของ Load Balancer
├── Dockerfile            # Multi-stage build สำหรับ Go App
├── docker-compose.yml    # Orchestration ของระบบทั้งหมด
├── index.html            # Frontend Dashboard (Tailwind + Chart.js)
├── loadtest.js           # สคริปต์ทดสอบ Load Test
├── go.mod                # Go Modules
└── ...

---

## 🔗 API Endpoints

### 1. Place Order (ซื้อสินค้า)

* **URL:** `/api/buy`
* **Method:** `POST`
* **Body:**
```json
{
  "user_id": 12345,
  "product_id": 1
}

```
* **Response:**
* `202 Accepted`: รับคำสั่งซื้อแล้ว กำลังประมวลผล (Queued)
* `400 Bad Request`: สินค้าหมด (Sold out)

### 2. WebSocket (Real-time Stock)
* **URL:** `/ws`
* **Description:** เชื่อมต่อเพื่อรับค่า `stock` ปัจจุบัน โดย Server จะ Push ข้อมูลมาให้ทันทีที่มีการเปลี่ยนแปลง
---

## ✨ Features Highlight

* **Atomic Stock Deduction:** ใช้ `DECR` ของ Redis เพื่อตัดสต็อก ป้องกันปัญหา Overselling ได้ดีเยี่ยม
* **Resiliency:** มีระบบ Retry Connection ในโค้ด (สำหรับ DB และ RabbitMQ) ทำให้ Container ไม่พังเมื่อ Start ไม่พร้อมกัน
* **Horizontal Scaling:** API Server ถูกตั้งค่าให้รัน 3 Replicas ใน Docker Compose เพื่อรองรับ Request จำนวนมาก โดยมี Nginx คอยเกลี่ยงาน
* **Graceful User Experience:** ผู้ใช้ได้รับ Feedback ทันทีไม่ต้องรอ Database Commit เสร็จ (Asynchronous)
